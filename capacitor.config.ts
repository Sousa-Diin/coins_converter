import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'coins_converter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
